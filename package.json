{
  "name": "ere.io",
  "version": "0.0.1",
  "description": "personal website",
  "main": "index.js",
  "repository": "https://github.com/ereio/ere.io",
  "author": "ereio",
  "license": "MIT",
  "scripts": {
    "start": "per-env",
    "start:production": "npm run -s serve",
    "start:development": "npm run -s dev",
    "build": "yarn generate && preact build --no-prerender",
    "serve": "serve ./dist",
    "serve:build": "preact build && serve build --single",
    "deploy": "yarn generate:dist && netlify deploy --dir=./dist/",
    "generate": "node ./scripts/generate-statics.js",
    "generate:dist": "./scripts/bundle-dist.sh",
    "deploy:prod": "yarn generate:dist && netlify deploy --prod --dir=./dist/",
    "deploy:prod:full": "yarn build && yarn generate:dist && netlify deploy --prod --dir=./dist/",
    "dev": "preact watch",
    "lint": "eslint src",
    "test": "jest",
    "postbuild": "react-snap"
  },
  "eslintConfig": {
    "extends": "eslint-config-synacor"
  },
  "eslintIgnore": [
    "build/*"
  ],
  "dependencies": {
    "marked": "^0.7.0",
    "normalize.css": "^8.0.1",
    "preact": "^10.0.1",
    "preact-markup": "^1.6.0",
    "preact-render-to-string": "^5.1.0",
    "preact-router": "^3.0.0"
  },
  "devDependencies": {
    "enzyme": "^3.10.0",
    "enzyme-adapter-preact-pure": "^2.0.0",
    "eslint": "^6.0.1",
    "eslint-config-synacor": "^3.0.4",
    "identity-obj-proxy": "^3.0.0",
    "jest": "^24.9.0",
    "jest-preset-preact": "^1.0.0",
    "per-env": "^1.0.2",
    "preact-cli": "^3.0.0-rc.6",
    "preact-cli-svg-loader": "^1.0.0",
    "preact-render-spy": "^1.2.1",
    "react-snap": "^1.23.0",
    "serve": "^11.1.0"
  },
  "jest": {
    "preset": "jest-preset-preact",
    "setupFiles": [
      "<rootDir>/tests/__mocks__/browserMocks.js",
      "<rootDir>/tests/__mocks__/setupTests.js"
    ]
  },
  "reactSnap": {
    "inlineCss": false,
    "source": "build",
    "destination": "build",
    "removeScriptTags": true
  }
}
